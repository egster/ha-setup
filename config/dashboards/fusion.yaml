# FUSION Dashboard — YAML-mode entry point
# Restructured by WP2 (Phase 7) — content split across config/dashboards/fusion/.
# DO NOT EDIT VIA HA UI — git is source of truth (DECISIONS.md 2026-04-24).
title: Fusion
button_card_templates: !include fusion/templates.yaml
views:
  - title: FUSION
    path: fusion
    type: panel
    cards:
      - !include fusion/shell.yaml
kiosk_mode:
  hide_header: "{{ is_state('input_boolean.fusion_kiosk', 'on') }}"
  hide_sidebar: "{{ is_state('input_boolean.fusion_kiosk', 'on') }}"
  hide_overflow: "{{ is_state('input_boolean.fusion_kiosk', 'on') }}"
