# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V7
.Trashes

# Backups (HA backup files if ever downloaded locally)
*.tar
*.gz

# Secrets — never commit these
secrets.yaml
.env
*.token

# Local rollback dumps (e.g. WP2 storage-mode dashboard backup) — local only
*.storage-backup.json

# Editor
.vscode/
*.swp

# Claude Code — machine-local permissions, not project config
.claude/settings.local.json

# Agent context — contains PII (address, household names, network details)
00 - Agent Context/PROFILE.md
00 - Agent Context/.backup/
00 - Agent Context/healthcheck*.json
00 - Agent Context/healthcheck*.md
Archive/
